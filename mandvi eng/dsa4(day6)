int findSum(int *arr, int l)
{
	int i , sum = 0;
	for( i = 0 ; i < l ; i++)
      	{
      		if( ( arr[i] % 2 == 0) || ( arr[i] % 3 == 0 ) )
	   	{
	   		sum = sum + arr[i];
	   	}
      	}
	return sum;
}
